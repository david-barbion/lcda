{% capture srcset %}
    {% for i in resized %}
      {{ site.url }}/{{ i.path }} {{ i.width }}w{% if forloop.last != true %},{% endif %}
    {% endfor %}
{% endcapture %}

{% assign largest = resized | sort: 'width' | last %}
{% assign smallest = resized | sort: 'width' | first %}

{% if title %}{{title}}{% endif %}<img src="{{ site.url }}/{{ smallest.path }}" alt="{{ alt }}" srcset="{{ srcset | strip_newlines }}"/>
